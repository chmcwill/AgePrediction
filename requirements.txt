# Core web framework
flask==3.1.2
gunicorn==23.0.0

# Image processing
# 4.12.0.88 pulls NumPy 2.x builds; pin to 4.10 to stay on NumPy 1.26.x
opencv-python==4.10.0.84
pillow==11.3.0
pillow_heif==1.1.1
matplotlib==3.9.4
numpy==1.26.4

# Deep learning (CPU wheels only) Python 3.11 compatible
# PyTorch 2.4.x ships CPU wheels on PyPI; no +cpu suffix needed
torch==2.4.1
torchvision==0.19.1
facenet-pytorch==2.5.3

# No extra index needed for CPU wheels on PyPI
